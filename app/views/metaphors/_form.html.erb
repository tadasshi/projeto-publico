<%= form_for(@metaphor, :html => {:multipart => true}) do |f| %>

    <% if @metaphor.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@metaphor.errors.count, "error") %> prohibited this metaphor from being saved:</h2>

          <ul>
            <% @metaphor.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :title %><br>
      <%= f.text_field :title %>
    </div>
    <div class="field">
      <%= f.label :description %><br>
      <%= f.text_area :description %>
    </div>
    <div class="field">
      <%= f.label :image %><br>
      <%= f.file_field :image %>
    </div>
    <div>
      <%= image_tag @metaphor.image_url(:thumb), class: 'img-responsive', alt: @metaphor.title if @metaphor.image? %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
